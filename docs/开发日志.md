# LintLizard 开发日志

> 板子：CH32V307V-EVT（丝印 `CH32F&V30xV-R1-1v1`，主控 CH32V307VCT6，板载 WCH-Link）
> 工程：本仓库根目录
> 仓库：https://github.com/tw-xy/LintLizard
> 记录时间：2026-09-14 ~ 09-19

## 当前状态（每次更新先改这里）

| 项 | 状态 |
|---|---|
| 阶段一：遥控（摇杆 → ESP → 协议 → 差速 → 串口打印） | ✅ 实测通过（方向全对 / 48 帧每秒 / err=0） |
| 阶段二：驱动电调（TIM3_CH1/CH2，50Hz / 1000–2000µs） | ✅ 寄存器级 + 推杆实测；**轮子转向已确认** |
| 阶段三：OLED 界面（无网络转圈 / 已连接 / 方向箭头） | ✅ 代码完成并烧录；屏幕已识别（`oled=1`）、状态机已跑通（`ui=direction dir=stop`）——**屏幕实际观感待确认** |
| 阶段四：扫地执行机构（边刷 EN + 吸尘电调） | 🟡 代码完成（`af9f1eb`），**硬件未接、固件未烧** |
| **下一步** | ① 接边刷/吸尘线 → 烧固件验 `clean=`/`vac=` ② **落地跑直线/原地转**（还没做） ③ 确认 OLED 显示效果 |
| 代码规模 | 自研约 2100 行（`User/ Bsp/ App/`，另含自动生成字模 259 行）；随仓库自带 `sdk/`（沁恒官方） |
| 构建 | `build.ps1`（Windows）/ `build.sh`（Linux、CI）；GitHub Actions 每次 push 自动编译两端 |
| 出厂固件 | `backup/backup_factory_20260911.bin`（294912 B；SHA256 与恢复命令见 `backup/README.md`） |

---

## 关于名字：为什么叫 LintLizard

**LintLizard** = **Lint**（毛絮、绒毛、灰尘）+ **Lizard**（蜥蜴）——
一只贴着地面爬行、专吃毛絮的蜥蜴，就是这台履带扫地机器人的形象。

* 名字定于 2026-09-15，仓库 `tw-xy/LintLizard`，**不改名**。
* 仓库名对应的是**整机**，不是当前阶段。现在仓库里只有第一阶段（遥控底盘）的功能，
  后面扫地、自动回充、避障导航、OLED/图传都往这个仓库里加，所以名字不会过时。
* 要区分版本时用 `LintLizard-v1`、`LintLizard-v2` 这种说法，仓库名保持不动。

---

## 0. 速查表

| 项目 | 值 |
|---|---|
| 主控 | CH32V307VCT6，LQFP100，HSE 8MHz → PLL 96MHz |
| SDK | `D:\WCH_CH32V307_EVT\extracted\EVT\EXAM\SRC`（官方外设库） |
| 工具链 | `...\MounRiver_Studio2\...\Toolchain\RISC-V Embedded GCC\bin`（riscv-none-embed-gcc 8.2.0） |
| 架构选项 | `-march=rv32i -mabi=ilp32`（与官方例程 obj 配置一致） |
| 烧录 | `...\WCH\OpenOCD\OpenOCD\bin\openocd.exe -f wch-riscv.cfg`（WCH-LinkE，SDI 两线） |
| 调试串口 | USART1 (PA9/PA10) → 板载 WCH-Link → **COM4 @115200** |
| 遥控串口 | USART3 (PB10/PB11) → ESP8266-01S @115200 |
| 电调输出 | TIM3_CH1=PA6、TIM3_CH2=PA7，50Hz / 1000~2000µs |
| 系统时基 | TIM2 1ms 中断（非阻塞时间片） |
| 仓库 | https://github.com/tw-xy/LintLizard（公开，`main` 分支，CI 自动编译两端） |
| 出厂固件备份 | `backup/backup_factory_20260911.bin`（294912 B） |

**三条命令**（都在工程目录下跑）：

```powershell
powershell -ExecutionPolicy Bypass -File .\build.ps1              # 编译 -> build\CAR_REMOTE.elf/.hex/.bin
powershell -ExecutionPolicy Bypass -File .\flash.ps1              # 烧录（会覆盖板子现有程序）
powershell -ExecutionPolicy Bypass -File .\log.ps1 -Seconds 60    # 录 COM4 -> logs\serial_*.log
```

Linux / CI 里用同样参数的 `bash build.sh`（GitHub Actions 就是这么编译两端的）。

---

## 1. 阶段一：娱乐与遥控（已完成 ✅）

### 1.1 数据链路

```
手机 Blinker 虚拟摇杆
     │ WiFi / 云
ESP8266-01S ──USART3(PB10/PB11,115200)──► CH32V307
                                            │ ① 行协议解析
                                            │ ② 差速解算
                                            ▼
                          USART1(PA9/PA10)=COM4 打印左右目标速度
```

### 1.2 引脚分配（依据：官方原理图 `PUB\CH32V30xSCH.pdf` 第 11 页 = VCT6-R1 板）

| 信号 | 引脚 | 板上位置 | 说明 |
|---|---|---|---|
| USART3_TX | PB10 | J4 第 1 脚（网络名 D15） | → ESP 的 RX |
| USART3_RX | PB11 | J4 第 3 脚（网络名 D14） | ← ESP 的 TX |
| GND | — | J4 第 7 脚 | 共地 |

**为什么选 USART3**：板上已被占用的资源 —— USART1(PA9/PA10)=WCH-Link 调试串口、PA13/PA14=SWD、
PA11/PA12=USB-HS、PB6/PB7=USB-FS+OLED、**PC6/PC7/PC8/PC9=内置 10M 以太网 PHY 差分线（接 HR911105A 网口）**、
PE9/PE7=用户 LED 跳线。USART3 的默认脚 PB10/PB11 完全空闲，且在 J4 上相邻、旁边就是 GND。

### 1.3 通信协议

115200 8N1，一行一帧，`\n` 结尾：

```json
{"x":-100,"y":100}
```

| 字段 | 范围 | 含义 |
|---|---|---|
| x | -100 ~ +100 | 左负右正（转向） |
| y | -100 ~ +100 | 后退负、前进正（油门） |

解析规则：只接受以 `{` 开头的行，只找 `x`/`y` 两个键，其它字段随便加。
ESP8266 上电时 74880 波特的启动乱码、Blinker 日志都会被安全丢弃（不占 err 计数以外的任何资源）。

### 1.4 差速解算

```
死区(±4%) → 指数曲线(可配) →  l = y + x ,  r = y - x  → 归一化 → 千分比输出(±1000‰)
```

归一化的作用：前进 100 + 右转 100 时 l=200/r=0，两边等比缩放到 l=1000‰/r=0‰，
**转向比例不失真**（不做归一化就会削掉一边、转角变化）。

### 1.5 非阻塞架构

* `BSP_Millis()`：TIM2 1ms 中断提供唯一时基；`BSP_Every(&last, period)` 做软定时器
* 主循环按 10ms（控制）/ 20ms（电调）/ 100ms（打印）/ 1s（统计）分片，**没有任何 `Delay_Ms()`**
* USART1/USART3 全部中断 + 环形缓冲；连 newlib 的 `_write()` 都重定向到环形缓冲，误用 `printf` 也不会死等
* 三层失效保护：ESP 端 1s 无摇杆数据回中 → 链路 300ms 无有效帧判离线 → 底盘 300ms 无新指令输出归零

### 1.6 实测验证数据

摇杆方向（一次完整推杆的记录）：

| 推杆方向 | x | y | L | R | 判定 |
|---|---|---|---|---|---|
| 正上（前） | +12 | **+100** | +999 | +784 | ✅ 前进为正 |
| 正下（后） | +3 | **-100** | -1000 | -1000 | ✅ 后退为负 |
| 正左 | **-99** | +20 | -677 | +999 | ✅ 左为负 |
| 正右 | **+100** | +7 | +999 | -868 | ✅ 右为正 |
| 右到底 | +100 | 0 | **+1000** | **-1000** | ✅ 完美原地右转 |

链路质量（连续运行 20s）：

```
[stat] LINK-OK  frames=2971 (48/s)  err=0  rx=41594  rxovf=0  txdrop=0
```

`frames` 单调递增从不归零 → 无重启；`err=0` → 一行都没解析错；两个缓冲零溢出。

### 1.7 遥控操作语义（怎么开、怎么停）

| 操作 | 行为 |
|---|---|
| 推摇杆 | 车按该方向**持续跑**（ESP 保持最后一次摇杆值，不会因为"按住不动"而掉速） |
| 把摇杆拖回中间 | **停车**（Blinker 摇杆只在手指移动时上报，拖动就会发 0,0） |
| 手机 / 云端断开 | ESP 检测到 `Blinker.connected()==false` → 自动归零 |
| 串口断了 / 主控异常 | CH32 侧 300ms 链路超时兜底 → 输出 0 并打印 `FAILSAFE` |

> ⚠️ 注意：**手指离开但没把摇杆拖回中间，车会保持最后速度继续跑**（"定速"语义）。
> 这是 Blinker 摇杆组件"事件式上报"的固有特性，不是 bug。
> 想要"按住才走、松手立停"的手感，要么换成局域网网页摇杆（能拿到触摸抬起事件），要么加"急停按钮"。

**实测验证（2026-09-15）**：改完后按住推杆 8 秒，`y` 稳定在 **+100** 从不归零，
脉宽保持 **1999/1943µs**，`frames` 单调递增（932→1268，48 帧/秒）、`LINK-OK` 全程稳定 → **"跑几秒就停"已解决**。

停车也一并验证：推住 3.5 秒（脉宽 1999/1909µs 不掉速）→ **把摇杆拖回中间** → `y` 立刻回 0、脉宽回 **1500/1500µs** → 再推又能启动。
开关车三段动作（开 → 停 → 再开）全部实测通过。

---

## 2. 阶段二：电调输出（信号层已验证 ✅，轮子转向待确认）

### 2.1 引脚

| 信号 | 引脚 | 板上位置 | 目标 |
|---|---|---|---|
| TIM3_CH1 | **PA6** | **J4 第 29 脚（丝印 D4）** | 左电调 信号线 |
| TIM3_CH2 | **PA7** | **J4 第 31 脚（丝印 D3）** | 右电调 信号线 |
| GND | — | J4 第 7 脚 | 两个电调的黑线 |

接线纪律（重要）：

1. **电调红线（BEC 5V/6V 输出）不接板子**，悬空包好
2. 电调动力电走**电池**（2S/3S 锂电），不要从板子 5V/3.3V 取
3. 电调 GND 必须与板子 GND 相连（信号要有参考电平）
4. 上电顺序：**先给板子上电**（程序跑起来输出中位）→ 再给电调上电池（电调读中位自检）

### 2.2 信号格式

标准 RC 有刷电调：**50Hz（20ms 周期）**，脉宽 `1000µs = 满倒车`、`1500µs = 中位(停)`、`2000µs = 满前进`。
上电后 3 秒（`MOTOR_ARM_DELAY_MS`）强制保持中位，给电调自检/解锁用。

### 2.3 寄存器级验证（openocd 直接读 TIM3）

```
CR1  = 0x81      CEN + ARPE
CCER = 0x11      CC1E | CC2E（两路输出使能）
PSC  = 0x5F(95)  96MHz / 96 = 1MHz → 1 计数 = 1µs
ARR  = 0x4E1F    19999 → 20ms = 50Hz
CCR1 = 0x5DC     1500µs（左，中位）
CCR2 = 0x5DC     1500µs（右，中位）
```

不用示波器也能自测：万用表直流电压档量 PA6/PA7 对 GND —— 中位 ≈0.25V、满前进 ≈0.33V、满倒车 ≈0.17V。

### 2.4 推杆实测（日志 `logs\serial_20260915_001224.log`）

| 时刻 | 动作 | x | y | L | R | 脉宽 | 判定 |
|---|---|---|---|---|---|---|---|
| 0.1s | 松手 | 0 | 0 | 0 | 0 | **1500/1500 µs** | ✅ 中位 |
| 4.1~6.2s | 推前 | ≈0 | +100 | +1000 | +1000 | **2000/2000 µs** | ✅ 满前进 |
| 11.7~12.7s | 推后 | -7 | -100 | -999 | -868 | **1001/1048 µs** | ✅ 满倒车 |
| 20.0~22.6s | 再推前 | ≈0 | +100 | +1000 | +1000 | **2000/2000 µs** | ✅ 复现 |
| 24.6~25.6s | 再推后 | -5 | -100 | -999 | -904 | **1001/1048 µs** | ✅ 复现 |

### 2.5 待办：轮子实际转向

把车架起来（履带悬空），逐个确认：

| 推杆 | 期望 |
|---|---|
| 前 | 两条履带都朝车头方向转 |
| 后 | 两条都朝车尾方向转 |
| 右 | 左履带往前、右履带往后（原地右转） |
| 左 | 左履带往后、右履带往前（原地左转） |

反了就把 `app_config.h` 里 `MOTOR_LEFT_INVERT` / `MOTOR_RIGHT_INVERT` 改成 1 重烧（比调线干净）。

---

## 3. 踩过的坑（下次直接看这节）

| # | 现象 | 根因 | 解决 |
|---|---|---|---|
| 1 | `.ps1` 脚本报"字符串未终止/意外标记" | Windows PowerShell 5.1 把**无 BOM 的 UTF-8 .ps1 当 GBK 解**，中文串把引号吃掉了 | 脚本里的字符串只用 ASCII（中文只留在注释外的地方/改成英文注释） |
| 2 | openocd 报 `couldn't open D:WCH_CH32V307_EVTprojects...` | openocd 的 `-c` 参数由 **Tcl 解析**，Windows 路径的反斜杠被当转义（`\b` 变退格） | 路径写成**正斜杠** `D:/.../x.elf` |
| 3 | 脚本一调用 openocd 就抛 `NativeCommandError` | openocd 把 banner 写到 **stderr**，而脚本设了 `$ErrorActionPreference='Stop'` | 调用前后临时设 `'Continue'`，用 `$LASTEXITCODE` 判断成败 |
| 4 | Arduino 开发板管理器装 ESP8266 包总是超时 | **Arduino CLI 不读系统代理/环境变量**，直连 GitHub 被打断 | 写 `%LOCALAPPDATA%\Arduino15\arduino-cli.yaml`：`network.proxy: http://127.0.0.1:7897` |
| 5 | 单独供电时点灯 APP 永远"不在线"，插上烧录器就在线 | ESP8266 连 WiFi 瞬时吃 **300~500mA**，弱电源直接塌陷 → 反复重启 | 独立 3.3V（≥500mA，5V+AMS1117-3.3），模块旁并 470µF+100nF；**USB-TTL 的 3.3V 带不动** |
| 6 | 用了带 `-5G` 的 5GHz SSID，怎么都连不上 | ESP8266 **只支持 2.4GHz** | 换成 2.4G 的 SSID |
| 7 | 烧录后 COM4 全是乱码，所有波特率都不对 | WCH-Link 的串口桥没跟目标对上（波特率本身没错：57600~921600 全扫过，只有 115200 正常） | **复位一次**（板上 S3 或 openocd reset）即恢复；判断依据：CPU halt 住后数据立刻归零 → 数据确实来自芯片 |
| 8 | COM4 突然消失，设备管理器里状态 `Unknown` | 板子断电/USB 掉枚举 | 插回 USB；下电后 COM4 消失是正常现象 |
| 9 | 原理图 PDF 文字读不出来 | Altium 导出的 PDF 文字被打散成单字符，按坐标拼不可靠 | 用 pypdfium2 **高倍渲染成图片**再读；或直接看关键区域的裁剪图 |
| 10 | Blinker 库编译报警告一大堆 | 库自身 `-Wwrite-strings` / `WiFiServer::available` 已废弃 | 与我们无关，忽略；我们自己的文件是 0 warning |
| 11 | ESP 上电串口先吐一段乱码 | ESP8266 ROM 用 74880 波特率打印启动信息 | 协议解析器只认 `{` 开头的行，自动丢弃 |
| 12 | 一次性命令里写 Windows 路径给原生程序，反斜杠会被吃掉 | PowerShell 向原生程序传参时的转义/引号处理 | 尽量用 `-File 脚本.ps1` 方式调用，路径在脚本内部处理 |
| 13 | 单独供电时 ESP 完全不启动（蓝灯不亮、串口无输出），插烧录器就正常 | **EN(CH_PD) 悬空** —— 我们这块模块没有可靠上拉（官方 ESP-01S 图上 R2 10K 是有的，但不是所有模块都焊/都有效）；另外蓝灯是接在 TXD/GPIO2 上的**数据灯**，不是电源灯 | **EN 直接接 3.3V**；判断模块死活看串口，不看灯 |
| 14 | 推杆后车**跑几秒自己停** | ESP 里"1 秒没收到摇杆数据就回中"的失效保护，把"按住不动"误判成"松手"（**Blinker 摇杆只在手指移动时上报**） | 改成保持最后值 + `!Blinker.connected()` 才归零；停车 = 把摇杆拖回中间 |

---

## 4. 参数速查（`App/app_config.h`）

| 宏 | 当前值 | 含义 |
|---|---|---|
| `DBG_UART_BAUD` / `ESP_UART_BAUD` | 115200 | 调试口 / 遥控口波特率 |
| `REMOTE_LINK_TIMEOUT_MS` | 300 | 多久没收到有效帧判定链路断开 |
| `CHASSIS_DEADZONE` | 4 | 摇杆死区（%） |
| `CHASSIS_EXPO_PCT` | 0 | 指数曲线强度（0=线性） |
| `CHASSIS_MAX_PERMILLE` | 1000 | 解算输出满量程（‰） |
| `CHASSIS_INPUT_TIMEOUT_MS` | 300 | 底盘兜底超时（无新指令→输出 0） |
| `CHASSIS_LEFT/RIGHT_INVERT` | 0 | 左右轮逻辑方向 |
| `MOTOR_PWM_PERIOD_US` | 20000 | 电调周期（50Hz） |
| `MOTOR_PULSE_MIN/NEUTRAL/MAX` | 1000/1500/2000 | 电调脉宽（µs） |
| `MOTOR_ARM_DELAY_MS` | 3000 | 上电保持中位时长（给电调自检） |
| `MOTOR_LEFT/RIGHT_INVERT` | 0 | 左右电调信号方向（装反了改 1） |
| `TASK_CTRL/MOTOR/DBG/STAT_PERIOD_MS` | 10/20/100/1000 | 各时间片周期 |

---

## 5. 代码结构与后续扩展点

```
User/main.c          只做时间片调度，不放业务逻辑
Bsp/bsp_time.c       TIM2 1ms 时基 + BSP_Every() 软定时器
Bsp/bsp_uart.c       USART1 调试口 / USART3 遥控口（中断 + 环形缓冲）
Bsp/bsp_syscalls.c   newlib 裁剪（_write 也走非阻塞缓冲）
Bsp/bsp_motor.c      TIM3_CH1/CH2 → 两路电调 PWM
App/app_remote.c     协议解析 + 链路超时（链路层）
App/app_chassis.c    差速解算（运动层，换底盘只动这里）
App/app_config.h     所有可调参数
```

| 以后的功能 | 加在哪 | 怎么接 |
|---|---|---|
| 扫地（主扫/吸尘/边刷） | 新增 `App/app_cleaner.c` | 输出走新的 `Bsp/bsp_motor` 通道或独立 PWM 引脚 |
| 自动回充（红外寻桩+触点） | 新增 `App/app_charge.c` | 通过 `Chassis_SetInput()` 直接控底盘 |
| 避障（ToF/超声波） | 新增 `App/app_avoid.c` | 在 `main.c` 里串在遥控与底盘之间做仲裁 |
| OLED / WS2812 状态显示 | 新增 `App/app_ui.c` | 只读状态，不阻塞 |
| 电机编码器测速 | 新增 `Bsp/bsp_encoder.c` | TIM 编码器模式，PID 放 `app_chassis.c` |

---

## 6. ESP8266 端（Arduino）

工程：`esp8266/ESP8266_Blinker_Joy/ESP8266_Blinker_Joy.ino`（仓库内；本机 Arduino 草稿本里另有一份同样的）

| 项目 | 值 |
|---|---|
| 开发板 | Generic ESP8266 Module |
| Flash Size | 1MB (FS:none OTA:~502KB) |
| Crystal / CPU | 26MHz / 80MHz |
| 依赖库 | Blinker 0.3.10230510（库管理器，作者 i3water） |
| 编译占用 | Flash 504672 B (48%)，RAM 35952 B (44%)，IRAM 92% |
| 发送节奏 | 20ms 一帧（50Hz）；1s 收不到摇杆数据自动回中 |
| 调参开关 | `Y_INVERT` / `X_INVERT`（方向反了改 1）、`TEST_SWEEP`（1=不连 WiFi 只发扫描值） |

---

## 7. 版本记录

| 日期 | 内容 |
|---|---|
| 2026-09-14 | 接线表定稿（USART3 PB10/PB11）；ESP8266 Blinker 摇杆代码完成并编译通过 |
| 2026-09-14 | 阶段一完成：协议解析 + 差速解算 + COM4 打印，方向与失效保护实测通过 |
| 2026-09-15 | 电调模块 `bsp_motor.c` 完成：TIM3 50Hz/1000-2000µs，寄存器级验证 + 推杆实测通过 |
| 2026-09-15 | 建 GitHub 仓库 `tw-xy/LintLizard`（公开）+ CI 自动编译两端；WiFi/密钥拆进 `secrets.h`（已 gitignore） |
| 2026-09-15 | README 写清 LintLizard 的名字来历与当前阶段；**仓库名保留不改**（对应整机，不是当前阶段） |
| 2026-09-15 | 仓库收尾：修掉失效指代（`.ino` 注释 → `docs/接线表.md`、开发日志工程路径 → 仓库根）；出厂固件 `backup/` 入库（附 SHA256 + openocd 恢复命令）；CI 连续通过 |
| 2026-09-15 | 轮子转向验证通过；修「跑几秒就停」（EN 必须接 3.3V + 去掉"1 秒无数据即回中"的误判），实测按住 8 秒不掉速 |
| 2026-09-15 | **OLED 界面完成**：字模（11 个汉字 + 8 方向箭头 + 停止图标，`tools/gen_glyphs.py` 生成）、软件 I2C SSD1306 驱动、三态界面状态机；协议加 `net` 字段；屏幕实测 `oled=1`、状态机 `ui=direction` 跑通 |
| 2026-09-15 | **扫地执行机构代码完成**：边刷 EN（PE8/PE10，12V→5V 模块高电平启动）+ 吸尘电调（PB0 = TIM3_CH3，车动 1250µs / 车停 1000µs，停后延时 2 秒再关）；`[stat]` 增加 `clean=`/`vac=` 便于无硬件验证 |
| 2026-09-19 | 恢复开发：更新「当前状态」；待办 = 扫地接线 + 烧录验证 / 落地跑直线与原地转 / OLED 观感确认 |
| 待办 | ① 扫地执行机构接线与实测 ② 落地跑直线、原地转 ③ 编码器闭环（远期） ④ 雷达 + 树莓派（中后期） |
